# URL publique de ton API (le backend emp-bot), sans slash à la fin.
# En local pour tester : http://localhost:3000
# Une fois hébergé (Railway, Render...) : https://ton-api.up.railway.app
VITE_API_BASE=http://localhost:3000
